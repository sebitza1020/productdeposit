CREATE TABLE product
(
id INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
nume VARCHAR(100),
unit VARCHAR(20),
CONSTRAINT primary_key PRIMARY KEY (id)
);

CREATE TABLE stock
(
stock_id INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
price_unit FLOAT,
quantity INTEGER,
CONSTRAINT primary_key1 PRIMARY KEY (stock_id),
CONSTRAINT foreign_key FOREIGN KEY (stock_id) REFERENCES product(id)
);

INSERT INTO product(nume,unit) 
VALUES('Prod1','Piece');

INSERT INTO stock(price_unit,quantity)
VALUES(5,1024);

INSERT INTO product(nume,unit) 
VALUES('Prod2','Kg');

INSERT INTO stock(price_unit,quantity)
VALUES(34,125);

INSERT INTO product(nume,unit) 
VALUES('Prod3','Meter');

INSERT INTO stock(price_unit,quantity)
VALUES(563,23);

INSERT INTO product(nume,unit) 
VALUES('Prod4','Liter');

INSERT INTO stock(price_unit,quantity)
VALUES(3.5,200);

SELECT product.ID, product.NUME, product.UNIT, stock.PRICE_UNIT, stock.QUANTITY 
FROM product INNER JOIN stock ON product.id=stock.stock_id ORDER BY nume, unit;

DROP TABLE stock;

DROP TABLE product;

/*SELECT * FROM product WHERE CAST(CAST(price_unit AS DECIMAL(10,2)) AS VARCHAR(20)) LIKE '%t%';*/
